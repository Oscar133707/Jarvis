# Dependencies
node_modules/

# Next.js
dashboard/.next/
dashboard/out/

# Env files — never commit secrets
.env
.env.local
.env.*.local

# Obsidian workspace cruft (keep the markdown, ignore the per-vault UI state)
vault/**/.obsidian/

# Claude Agent SDK runtime artifacts
**/.claude/

# OS
.DS_Store
Thumbs.db
