---
name: General
status: active
---

# General (DM)

This is Jarvis's general knowledge base, used when you talk to it in a **direct message** (not a project
channel). Personal notes, habits, cross-project thinking. Agents read here and record useful things into
`notes/`.

(No `slackChannel:` on purpose — this context is only used for DMs, not bound to a channel.)
